apiVersion: v1
kind: ConfigMap
metadata:
  name: database-config 
  namespace: default
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
data:
  # Key-value pairs for simple settings
  database_host: "db.example.com"
