### sync state
It defines, whether the live state is matching with the desired state of the git repository

### health checks
argocd will run the built in k8s health checks on the manifests it has deployed and results in whether the health is OK or NOT.